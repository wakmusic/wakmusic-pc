import styled from "styled-components";

interface AppProps {}

const App = ({}: AppProps) => {
  return (
    <Container>
      <h1>Hello, World!</h1>
    </Container>
  );
};

const Container = styled.div``;

export default App;
